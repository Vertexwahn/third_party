"""autodiff build and test dependencies."""

module(
    name = "autodiff",
    version = "1.0.3",
    compatibility_level = 1,
)

bazel_dep(name = "eigen", version = "5.0.1")
bazel_dep(name = "rules_cc", version = "0.2.16")

bazel_dep(name = "catch2", version = "3.12.0", dev_dependency = True)
